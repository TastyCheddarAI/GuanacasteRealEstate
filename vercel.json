{
  "buildCommand": "mkdir -p apps/web/packages && cp -r packages/* apps/web/packages/ && cd apps/web && npm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}